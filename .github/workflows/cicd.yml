name: Bank Frontend Pipeline

on:
  push:
    branches:
      - main

jobs:
  Continuous-Integration_build_and_push:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          repository: sam-osung/banking-app-with-k8s
          ref: main

      # 2️⃣ Configure AWS credentials
      - name: Configure AWS CLI
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      # 3️⃣ Debug runner workspace
      - name: Debug runner workspace
        run: |
          pwd
          ls -al
          ls -al Dockerfile

# Uncomment and fix the Docker/ECR steps when ready:
#      # 4️⃣ Build Docker image
#      - name: Build container image
#        run: docker build -t bank-app-frontend:latest .
#        working-directory: ${{ github.workspace }}

#      # 5️⃣ Log in to ECR
#      - name: Log in to ECR
#        run: |
#          aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin ${{ vars.ECR_REPO }}

#      # 6️⃣ Tag Docker image
#      - name: Tag Docker image
#        run: docker tag bank-app-frontend:latest ${{ vars.ECR_REPO }}/dev-bankfrontend:${{ github.run_number }}

#      # 7️⃣ Push Docker image
#      - name: Push image to ECR
#        run: docker push ${{ vars.ECR_REPO }}/dev-bankfrontend:${{ github.run_number }}

#      # 8️⃣ Clean up local Docker images
#      - name: Clean up Docker images
#        run: |
#          docker rmi bank-app-frontend:latest
#          docker rmi ${{ vars.ECR_REPO }}/dev-bankfrontend:${{ github.run_number }}
